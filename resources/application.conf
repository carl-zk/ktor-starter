ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [com.example.config.KodeinKt.kodein, com.example.config.HttpKt.http,com.example.config.RouterKt.routers]
    }
    env = dev
    env = ${?ENV}
}

db {
    url = "jdbc:mysql://localhost:3306/jooq_learn"
    user = "root"
    password = "password"
}
